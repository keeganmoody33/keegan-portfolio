COMPLETED (2026-08-22 session — live Supabase sync execution):
- Received owner sign-off and executed `sql/2026-08-22_sync_recent_experiences.sql` against production project `cvkcwvmlnghwwvdqudod` via equivalent PostgREST calls.
- Synced four experiences, bringing `experiences` to 15 rows ordered by `display_order`; inserted 11 new skills, bringing `skills` to 25 rows.
- Refreshed the coding gap to `Learnable Weakness` / `Write production code` with the current actively-developing context.
- Verified the post-migration production state and retained the SQL file as the canonical, idempotent migration record.

COMPLETED (2026-08-22 session — portfolio audit + schema sync):
- Audited AssetMule, BCOFA 2025, Kivira, and Morph experience markdowns against DATABASE_UPDATES.md
- Flagged unverified metrics ($200K early-stage pipeline, 20+ net-new leads, 285 validated contacts, AssetMule revenue/reply rates, Morph publication sign-off)
- Wrote honest_context caveats for all four new rows consistent with "honest over polished"
- Corrected schema drift in DATABASE_UPDATES.md (candidate_profile singular, company_name/role_title/public_bullets/private_context_what_id_do_differently, skills category/proficiency_level, gaps_weaknesses robust fallback)
- Produced consolidated idempotent migration: sql/2026-08-22_sync_recent_experiences.sql
- Updated current-state docs and timestamps: PROJECT_CONTEXT.md, DATABASE_UPDATES.md, README.md, CLAUDE.md, lessons.md, progress.txt
- Created CONTENT_SYNC.md with the single source of truth + recurring cadence pipeline
- Opened PR for owner sign-off; SQL execution gated until Keegan approves

COMPLETED:
- Single-page portfolio (app/page.tsx)
- Hero with SprayText animation (components/SprayText.tsx)
- Career timeline (components/Timeline.tsx)
- JD Analyzer with Firecrawl URL scraping (components/JDAnalyzer.tsx, app/api/jd-analyzer/route.ts)
- AI Chat via Supabase Edge Functions + Claude API (components/Chat.tsx, app/api/chat/route.ts)
- PostHog analytics (client + server)
- Marquee ticker (components/Marquee.tsx)
- Publications section (components/Publications.tsx, used inside Timeline)
- Vercel deployment with URL rewrites (vercel.json)
- Discogs "Recent Digs" banner widget (components/RecentDigs.tsx, app/api/discogs/route.ts)
- Canonical documentation: PRD.md, APP_FLOW.md, TECH_STACK.md, FRONTEND_GUIDELINES.md, BACKEND_STRUCTURE.md, IMPLEMENTATION_PLAN.md
- DESIGN_PLAYBOOK.md content folded into FRONTEND_GUIDELINES.md
- Page title updated to 'Keegan Moody | lecturesfrom' (LEC-25)
- Hero taglines updated to Set Q: Revenue systems / Useful in every room / Here for the long build (LEC-26)
- Footer: added Discord + Bluesky social links (LEC-27)
- Marquee: replaced fake data with real GitHub API via /api/github route (LEC-28)
- FRONTEND_GUIDELINES.md synced to match actual component behavior (LEC-29)
- Supabase experiences: fixed dates (TraceAir, Chapel Hill), title (Barbour), enriched bullets (Mixmax, Biofourmis), added Mercer + EMT rows (LEC-30)
- Supabase MCP config fixed: project_ref and read_only flag corrected
- YouTube persistent player widget (Phase 1, Step 1.1) -- components/YouTubePlayer.tsx, types/youtube.d.ts
- GitHub activity widget (Phase 1, Step 1.2) -- components/GitHubActivity.tsx, app/api/github/route.ts
- Banner compaction (LEC-31): RecentDigs covers 250px→72px, GitHubActivity chart 48px→24px, YouTubePlayer tightened, all widgets py-2 with inline labels
- YouTubePlayer: fixed persistState stale closure (reads player.getPlayerState() instead of React state)
- YouTubePlayer: fixed Script deadlock (<Script> + hidden iframe now always render, skeleton is a ternary inside same return)
- BannerRotator (LEC-32): YouTubePlayer, RecentDigs, GitHubActivity cycle in single rotating slot below Marquee — 8s auto-rotate, pause-on-hover, dot indicators
- YouTubePlayer: fixed strict-mode container destruction (LEC-33) — YT.Player replaces <div> with <iframe>, destroy() removes it from DOM, React doesn't know. Fix: wrapperRef + re-create container in initPlayer() if missing.
- Banner widget bug fixes: RecentDigs, GitHubActivity, and Marquee now forward X-POSTHOG-DISTINCT-ID to API routes when PostHog has a real distinct ID; GitHubActivity loading skeleton and YouTubePlayer iframe container id are deterministic to avoid hydration mismatch.
- Phase 1, Step 1.4 (Banner Section Layout) -- DONE, shipped via BannerRotator with 3 widgets (YouTube, RecentDigs, GitHub)

COMPLETED:
- Phase 2: Turntable loading experience — shipped as root `/` gate
  - Exported turntable.blend → public/models/turntable.glb
  - Rewrote TurntableCanvas.tsx to load GLB, spin platter/record group, pivot tonearm group
  - Moved portfolio from `/` to `/keeganmoody33`, turntable gate now at `/`
  - Updated vercel.json to remove old `/` → `/keeganmoody33` redirect/rewrite
  - Updated APP_FLOW.md and TECH_STACK.md to match new routing

COMPLETED (2026-07-24 session — debt + polish):
- Rate limiting on all 4 API routes (lib/rate-limit.ts): chat/jd-analyzer 10/min, discogs/github 30/min, 429 + Retry-After + PostHog tracking
- Discogs API route: added 5-min cache (next: { revalidate: 300 })
- .env.local.example: added DISCOGS_TOKEN entry
- ARIA labels added: YouTubePlayer progress bar (role=slider), Marquee (aria-label), Chat (send/input/clear/suggested), JDAnalyzer (submit/textarea), BannerRotator dots, Timeline company links, page nav/CTA buttons
- Mobile responsive fixes: hero text (text-5xl sm:text-7xl), padding (px-4 sm:px-8), nav gap, footer stacking, Timeline date column/card padding, Chat send button
- ActivityStream component REMOVED from app/keeganmoody33/page.tsx (fake data undermined credibility; GitHub Activity widget covers real activity)
- APP_FLOW.md updated: removed Activity Stream from render order + interaction flows
- Edge Functions modernized: replaced deprecated std@0.168.0/http/server.ts with built-in Deno.serve(); pinned @supabase/supabase-js@2.90.1
- Edge Functions deployed: chat v14, jd-analyzer v13 (both live on Supabase)
- Schema drift fixed in archived SQL files: candidate_profiles→candidate_profile, bullet_points→public_bullets, key_deliverables→public_bullets, honest_assessment→private_context_what_id_do_differently
- All docs scrubbed: Worthy Reads and Alan Iverson removed/cancelled across PRD, IMPLEMENTATION_PLAN, BANNER_WIDGETS_SPEC, AURA_PROMPTS, DESIGN_PLAYBOOK, TURNTABLE_LOADING_SPEC, NAVIGATION_PATHWAYS_SPEC, HUMAN_MACHINE_TOGGLE_SPEC, lessons.md
- docs/ALAN_IVERSON_SPEC.md deleted
- TECH_STACK.md updated with Edge Function changes
- Commit 2e9fc67 pushed to origin/main, Vercel auto-deployed, site live (200 OK)

CANCELLED:
- Worthy Reads widget (Phase 1, Step 1.3) -- CANCELLED 2026-07-24, feature descoped
- Alan Iverson chat persona (Phase 3) -- CANCELLED 2026-07-24, feature descoped

NEXT:
- Phase 4: Navigation pathways redesign + Vinyl grid overlay (spec exists: docs/NAVIGATION_PATHWAYS_SPEC.md)
- Phase 5: Human/Machine toggle (spec exists: docs/HUMAN_MACHINE_TOGGLE_SPEC.md, depends on Phase 4)

--- TURNTABLE HANDOFF (Phase 2) ---

This section provides full context for any agent picking up the Turntable build.

GOAL: Entry experience at lecturesfrom.com/keeganmoody33. Visitor sees a turntable
loading page. They interact with it (drop the needle / click play). Music starts via
YouTube, then the portfolio loads with music continuing in the banner player.

WHAT EXISTS ALREADY:
- YouTubePlayer widget: components/YouTubePlayer.tsx
  - Loads YouTube IFrame API client-side via next/script
  - Hidden iframe, custom retro control bar
  - Playlist: PLK7yHtEENYGHUVVhW9oaFVKRhh-FORGOk
  - sessionStorage contract at key 'yt-player-state'
  - Persists: videoId, trackTitle, trackAuthor, position, duration, playing, playlistIndex, volume, timestamp
  - On mount, checks sessionStorage -- if playing=true and timestamp is fresh (<30s), resumes playback
- TypeScript types: types/youtube.d.ts (YT namespace declarations)
- Error boundary: WidgetErrorBoundary in app/page.tsx wraps all banner widgets

AUTOPLAY STRATEGY (CRITICAL):
- Browsers block audio autoplay UNLESS preceded by a user gesture (click/touch)
- The turntable needle drop IS the user gesture. User clicks/taps to drop the needle.
- This click event triggers player.playVideo() -- browser allows it because it's
  user-initiated. No autoplay workaround needed.
- Flow: user click -> playVideo() -> music starts -> transition to portfolio
- The banner YouTubePlayer reads sessionStorage on mount and continues playback

WHAT NEEDS TO BE BUILT:
1. Turntable page/component (MVP version for tonight, polish later in Aura)
   - Dark page, centered turntable visual or play interaction
   - Needle drop / click triggers YouTube playback
   - Writes to sessionStorage['yt-player-state'] with playing: true
   - Transition animation (fade/zoom) to portfolio
   - Skip button with clever copy (not "skip" -- something that makes user feel lame)
   - Return visitor bypass: check localStorage, skip to portfolio if visited before
   - prefers-reduced-motion: skip turntable entirely
2. Routing: turntable as entry point, portfolio loads after interaction
3. PostHog events: turntable_loaded, turntable_needle_dropped, turntable_skipped

SPEC: docs/TURNTABLE_LOADING_SPEC.md (full locked spec)
SESSION STORAGE KEY: 'yt-player-state' (match YouTubePlayer.tsx exactly)
LOCAL STORAGE KEY: 'lf-visited' (for return visitor bypass)

DEPLOYMENT STRATEGY:
- Build on branch: feature/turntable-loading
- Push branch -> Vercel creates preview URL for testing
- Test preview URL on desktop + mobile
- Merge to main only when verified
- Rollback: git revert HEAD --no-edit && git push origin main

KNOWN ISSUES:
- achievements table populated but not queried by any Edge Function
- In-memory rate limiting won't work across multiple Vercel serverless instances (stopgap; needs Upstash Redis or Vercel KV for production-grade)
- components/ActivityStream.tsx still exists on disk but is no longer imported or rendered (safe to delete if confirmed not needed)

RESOLVED (2026-07-24 session):
- ~~Schema drift in SQL files~~ — fixed: candidate_profiles→candidate_profile, bullet_points→public_bullets, key_deliverables→public_bullets
- ~~Deno std lib pinned to 0.168.0~~ — replaced with built-in Deno.serve()
- ~~No ARIA labels~~ — added to all interactive elements
- ~~No rate limiting~~ — added to all 4 API routes
- ~~No caching on Discogs~~ — added 5-min revalidation
- ~~Activity Stream hardcoded data~~ — component removed from page
- ~~DISCOGS_TOKEN missing from .env.local.example~~ — added

PARKING LOT (not in scope yet):
- Human/Machine toggle (Phase 5)
- Navigation pathways redesign (Phase 4)
- Vinyl grid overlay / Stack of Wax (Phase 4)
- Easter eggs (Konami code, hidden /practice route)
- Console.log personality messages

---

## Tuesday, February 10th, 2026 — 7:35 PM - US-002: Create YouTube player service for loading page
- Created lib/youtube-service.ts with full YouTube player service
- Exports: createPlayer(), playVideo(), saveStateToSession(), destroyPlayer(), etc.
- Session storage state format matches YouTubePlayer.tsx exactly (yt-player-state key)
- Supports onReady, onStateChange, onError callbacks
- Proper cleanup/destroy handling with active player tracking
- Added waitForYouTubeApi() helper for async initialization
- Added getHandoffState() for banner player handoff with 30s freshness check
- Created comprehensive test suite: lib/youtube-service.test.ts (34 tests, all passing)
- Critical: playVideo() is designed for synchronous click handler invocation
- Updated tsconfig.json to exclude *.test.ts files from typecheck
- Build passes, typecheck passes, all tests pass

**Learnings:**
- Session storage contract must match between loading page and banner player
- PLAYLIST_ID: PLK7yHtEENYGHUVVhW9oaFVKRhh-FORGOk
- SESSION_KEY: 'yt-player-state'
- State format: { videoId, trackTitle, trackAuthor, position, duration, playing, playlistIndex, volume, timestamp }
